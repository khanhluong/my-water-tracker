class WaterEntry {
  time: string;
  amount: number;
  type: string;

  constructor(time: string, amount: number, type: string) {
    this.time = time;
    this.amount = amount;
    this.type = type;
  }
}
